#include "main.h"

/**
 * _islower - check for lowercase character
 * @c: character to lookout
 * Return: 1 if c is lowercase and 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
